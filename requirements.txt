numpy>=1.26
scipy>=1.11
rasterio>=1.3.9
pyproj>=3.6
shapely>=2.0
requests>=2.31
tqdm>=4.66
Pillow>=10.0
# The landfire package declares Requires-Python <3.12 (stale packaging pin; it runs
# fine on 3.13). It is NOT listed above because it would abort this whole file on
# modern Pythons. Install it separately:
#   pip install landfire --ignore-requires-python --only-binary=:all:
