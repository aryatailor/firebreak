numpy>=1.26
# serve.py (the demo server + free-play API). Pinned; demo.ps1 falls back to
# python -m http.server if these are missing, so the demo runs either way.
fastapi==0.115.6
uvicorn==0.34.0
scipy>=1.11
rasterio>=1.3.9
pyproj>=3.6
shapely>=2.0
requests>=2.31
tqdm>=4.66
Pillow>=10.0
# The landfire package is NOT needed: its LFPS endpoint was retired (see Deviations
# in implementation-notes.md). fetch_data.py talks to the live lfps.usgs.gov/api
# REST service directly with requests.
