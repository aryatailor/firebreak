# Overnight pipeline session (ended 09:50, stopped early for the PR merge)

1. **Tier 1 SHIPPED** - `physics.json` + `parity.json` in both town dirs, and the full browser-side algorithm (neighbour order, slope/wind formulas, how cleared cells apply, what "reached" means) written into CONTRACT.md. `python pipeline/verify_parity.py web/data web/towns/altadena` reimplements the model from physics.json alone and matches **100% exactly** on all four arrays.
2. **Tier 2 SHIPPED** - crawl lines for both towns (verbatim as given) in `meta.crawl`, `story:true` in `web/towns/index.json`, both documented in CONTRACT.md. Note the crawl says 50/60 mph because those are the real-world gusts; the model runs the calibrated 35/45 mph, and CONTRACT.md says so explicitly.
3. **Tier 3 SHIPPED** - `serve.py` (FastAPI, pinned in requirements.txt) with `/api/health`, `/api/region`, `/api/region/{id}/status`, `/api/simulate`, plus `demo.ps1` (`-Offline`, `-NoBrowser`, falls back to `http.server` if FastAPI is absent). Health, static files and simulate were tested live and answer correctly; `--no-solve` free-play paths added to `export.py`, `run_all.py`, and new `pipeline/region.py`.
4. **Tier 3 UNVERIFIED** - the one thing I did not get to prove is an end-to-end `/api/region` build. The code path is complete and `towns/santa-rosa-ca.json` was written by it before I stopped, but **no region has ever been built to completion**, so treat free-play region search as untested.
5. **Tier 4 NOT DONE** - no pre-built regions (Santa Rosa, Superior/Louisville, Talent/Phoenix). Santa Rosa has a ready config; the other two do not. Nothing was written into `web/` for any of them.
6. **Tier 5 NOT DONE** - no DEMO.md talk track, no `deck/firebreak.pptx`, and README/PLUME still do not mention free play or the region API.
7. **Tier 6 NOT DONE** - no clean-cache `--force` rebuild, no repeat parity/size audit beyond the checks above.
8. **Morning command: `.\demo.ps1`** (add `-Offline` at the venue). If FastAPI is missing anywhere, `pip install -r requirements.txt` or just `python -m http.server -d web 8000` - every exported town works on a plain static server, which is how the demo ran all night.
9. **Check this first:** start `.\demo.ps1`, confirm Paradise plays through, then type a town into the free-play search box - that box only appears when `/api/health` answers, and the build behind it is the one untested path. If it misbehaves, stop `serve.py` and use the static server; story mode is unaffected.
10. **Deviation logged** (implementation-notes.md): the per-town web budget went 20 -> 25 MB because physics.json + parity.json pushed Paradise to 20.30 MB; keeping every required file beat degrading data to satisfy a guardrail meant to keep the repo clonable. Altadena is 8.10 MB.
